- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.config/kitty: kitty
    ~/.editorconfig: editorconfig/editorconfig

# - create:

- shell:
  - [git submodule update --init --recursive, Installing submodules]
